# -neural-latent-aligner

## Initial repository setup

```bash
git init
git remote add origin https://github.com/<org>/<repo>.git
git add . && git commit -m "Initial commit"
git push -u origin main
```